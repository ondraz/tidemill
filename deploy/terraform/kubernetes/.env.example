# Secrets for Terraform — sourced before running terraform commands.
# Copy to .env and fill in values. Do NOT commit .env.
#
# Usage: set -a && source .env && set +a && terraform apply

TF_VAR_hcloud_token=""           # Hetzner Cloud API token (https://console.hetzner.cloud → Security → API Tokens)

# Copy to .env.local for local development.
# Get your key from https://dashboard.clerk.com → API Keys → choose React.
# Leave unset to disable auth (API runs in public mode).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
